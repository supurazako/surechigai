# Web Viewer

端末ごとの設定・閲覧画面です。単独で配信するWebアプリではなく、CLIバイナリに埋め込まれます。

```sh
cd ../cli
cargo run --locked -- --web
```

起動後に `http://127.0.0.1:8787` を開き、ユーザー名と配布する6文節を入力して交換を開始します。BLE通信と文章の状態はCLIプロセス内にあり、外部サーバーへ送信しません。
